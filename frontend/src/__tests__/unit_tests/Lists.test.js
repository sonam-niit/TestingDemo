import { render } from "@testing-library/react";
import Lists from "../../components/Lists";

describe("Lists.js render test", () => {
    test('should render Lists.js properly', () => {
        render(<Lists />)
    });
});